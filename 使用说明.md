# 目录导出管家 使用说明

## 一、安装

### 方式 A：源码运行（推荐先试）

1. 安装 [Python 3.10+](https://www.python.org/downloads/windows/)，安装时勾选 **Add Python to PATH**。
2. 双击 `安装到电脑.bat`（复制程序到 `%LOCALAPPDATA%\CatalogExporterApp` 并设置开机自启）。
3. 若只想临时运行，可双击 `启动后台服务.bat`，之后在“设置”里勾选开机自启。
4. 右键托盘图标 → “打开主界面”浏览索引。

### 方式 B：打包 exe

1. 双击 `build_exe.bat`（需联网安装 PyInstaller）。
2. 把整个 `dist\CatalogExporter` 文件夹放到固定目录（例如 `D:\Software\CatalogExporter\`）。
3. 运行 `CatalogExporter\CatalogExporter.exe install` 设置开机自启；之后每次开机自动在托盘运行。

## 二、首次使用

1. 程序启动后常驻托盘，无需打开界面。
2. 插入外接硬盘，几秒内自动开始扫描；几十万文件可能需要几分钟到几十分钟。
3. 扫描完成后托盘弹出“索引完成”轻提示。
4. 可以随时拔掉硬盘，之后双击托盘图标打开主界面浏览。

## 三、浏览与搜索

- 左侧是硬盘和目录树；右侧显示当前目录的文件与子目录。
- 双击右侧目录行，可以进入该目录。
- 顶部搜索框支持按文件名或完整路径关键字搜索。
- “类型”输入 `mp4`、`pdf` 等可精确过滤。
- “大小”支持 `KB`、`MB`、`GB` 后缀，例如 `500MB`。
- “日期”格式为 `2026-01-01`，可填起止日期。
- 点击右侧表头可排序；再次点击切换升降序。

## 四、自动扫描规则

- 默认自动扫描：所有本地固定盘 + 可移动盘（包括系统盘、USB 硬盘盒、U 盘）。
- 如果某块盘不想被扫描，在“设置 → 忽略的盘符”里填写盘符，例如 `D:`。
- 隐藏文件、系统文件默认不扫描，可在设置中打开。
- 软件未运行时插入的硬盘，下次启动软件时会自动补扫。

## 五、扫描中断与续扫

- 扫描中拔出硬盘，已扫描部分会保留，硬盘状态显示“部分索引”。
- 下次插入同一块硬盘，自动从断点继续，不会从头开始。
- 如需强制完整重扫，在主界面点击“重新完整扫描”，或在命令行执行 `python main.py scan --disk <id> --full`。

## 六、HTML 离线报告

1. 主界面选中硬盘，点击“导出 HTML”。
2. 生成的是一个自包含的 `.html` 文件，双击即可用浏览器打开。
3. 报告左侧是目录树，点击目录或右侧的文件夹行可逐层浏览；顶部有搜索、类型/大小/日期筛选、排序和分页，全部本地运行，无需联网。
4. 可以把报告拷到手机、U 盘或网盘，随时随地查目录。
5. 如果想自动保存：打开“设置 → 自动导出 HTML 报告”，勾选“开机时自动导出”或“插入硬盘扫描完成后自动导出”，并选择一个保存文件夹；程序会在对应时机自动把报告保存进去。每块盘会保存到“盘符盘_usb_序列号”子文件夹里（检测到 USB 时带 usb，例如 I 盘 → `i盘_usb_6e42646d\`；内置盘如 C 盘 → `c盘_6eb6916a\`），不同硬盘即使盘符相同也不会混在一起，文件名带硬盘名、序列号和时间。

## 七、数据与隐私

- 索引只保存在本机 `%APPDATA%\DiskMenu\index.db`，程序不上传任何数据。
- 主界面“设置”里可以设置打开界面密码（用于防止他人打开软件查看索引；这是界面访问保护，不是数据库磁盘级加密，如需更高安全建议配合 Windows BitLocker/账户加密）。
- 删除某块硬盘索引：主界面点击“删除索引”，或命令行 `python main.py delete --disk <id>`。
- 清空全部：`python main.py clear`。
- 彻底删除所有数据：关闭程序后删除 `%APPDATA%\DiskMenu` 文件夹。

## 八、常见问题

### 插盘后没有自动扫描？

- 确认托盘程序正在运行（任务栏右侧有图标）。
- 确认该盘不是系统盘且没有被加入“忽略的盘符”。
- 打开主界面看硬盘状态；若显示“未连接”，说明程序没识别到，稍等 3 秒会轮询重试。
- 若移动过程序文件夹，开机自启指向的旧路径会失效，请重新运行 `安装到电脑.bat` 或 `python main.py install`。

### 扫描很慢 / 占用高？

- 大目录扫描是正常现象；程序按目录逐个写入数据库，普通办公电脑也能在后台低占用运行。
- 不要频繁插拔；扫描中拔出会保留已扫描部分，下次自动续扫。

### 为什么我插上的是移动硬盘，但程序没扫描？

某些硬盘盒被识别为“固定盘”，设置里默认会扫描非系统固定盘，通常没有问题。若仍未扫描，在“强制视为外接盘的盘符”里填上该盘符。

### 为什么我看到的文件比硬盘上少？

- 默认跳过隐藏文件、系统文件、符号链接/快捷方式。
- 遇到权限不足或坏道的目录会跳过并记录，不影响整体扫描。
- 可在设置中打开隐藏/系统文件后重新完整扫描。

### 拔盘后能修改索引吗？

索引只是“目录快照”，拔盘后仍可浏览、搜索、排序、筛选和导出。重新插盘并扫描后会自动更新为最新状态。

### 忘记界面密码怎么办？

目前密码用于主界面打开验证。忘记密码时，关闭程序后删除 `%APPDATA%\DiskMenu\index.db`（会同时清空全部索引），重新扫描即可。
